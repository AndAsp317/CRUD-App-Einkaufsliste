<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Item bearbeiten</title>
  <style>
    body {
      font-family: "Segoe UI", sans-serif;
      background-color: #f6f8fa;
      color: #333;
      max-width: 500px;
      margin: 2rem auto;
      padding: 1rem;
    }

    h1 {
      text-align: center;
      margin-bottom: 2rem;
      color: #2c3e50;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 1rem;
    }

    input[type="text"] {
      padding: 0.6rem;
      border: 1px solid #ccc;
      border-radius: 4px;
      font-size: 1rem;
    }

    button {
      padding: 0.6rem 1rem;
      background-color: #27ae60;
      border: none;
      color: white;
      border-radius: 4px;
      cursor: pointer;
      font-size: 1rem;
      transition: background-color 0.3s;
      width: fit-content;
    }

    button:hover {
      background-color: #1e8449;
    }

    a {
      margin-top: 1rem;
      display: inline-block;
      text-decoration: none;
      color: #3498db;
      font-weight: bold;
      transition: color 0.3s;
    }

    a:hover {
      color: #21618c;
    }

    @media (max-width: 500px) {
      body {
        margin: 1rem;
      }

      button {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <h1>✏️ Item bearbeiten</h1>

  <form action="/edit/<%= index %>" method="POST">
    <input type="text" name="item" value="<%= text %>" required />
    <button type="submit">Speichern</button>
  </form>

  <a href="/">← Zurück zur Einkaufsliste</a>
</body>
</html>
